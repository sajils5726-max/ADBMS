CREATE TABLE Customer(CustomerID INT PRIMARY KEY,Name VARCHAR(50),City VARCHAR(30));

CREATE TABLE Product(ProductID INT PRIMARY KEY,ProductName VARCHAR(50),Price NUMBER(10),CustomerID INT,FOREIGN KEY (CustomerID) REFERENCES Customer(CustomerID));

INSERT INTO Customer VALUES (1,'Alice','Kochi');
INSERT INTO Customer VALUES (2,'Babu','Trivandrum');
INSERT INTO Customer VALUES (3,'Chitra','Kollam');
INSERT INTO Customer VALUES (4,'siva','Kottayam');
INSERT INTO Customer VALUES (5,'sandhya','Trivandrum');

INSERT INTO Product VALUES (101,'Laptop', 5500, 1);
INSERT INTO Product VALUES (102,'Mobile', 15000, 2);
INSERT INTO Product VALUES (103,'Tablet', 20000, 3);
INSERT INTO Product VALUES (104,'Printer', 10000, 4);
INSERT INTO Product VALUES (105,'Monitor', 12000, 5);

SELECT * FROM Product;

SELECT Name, City FROM Customer1;

SELECT * FROM Customer WHERE City = 'Kochi';

SELECT * FROM Product WHERE Price > 15000;

SELECT CustomerID, COUNT(*) AS TotalProductsFROM Product
GROUP BY CustomerID;

SELECT CustomerID, AVG(Price) AS AvgPriceFROM Product
GROUP BY CustomerID;

SELECT CustomerID, AVG(Price) AS AvgPriceFROM Product
GROUP BY CustomerID HAVING AVG(Price) > 12000;


SELECT * FROM Product ORDER BY Price ASC;


SELECT * FROM Customer ORDER BY Name DESC;


SELECT * FROM Customer WHERE CustomerID IN (SELECT CustomerID FROM Product WHERE Price > 15000);


SELECT * FROM Product WHERE Price = (SELECT MAX(Price) FROM Product);


CREATE VIEW ProductCustomer AS SELECT p.ProductName, p.Price, c.Name AS CustomerName FROM Product p JOIN Customer c ON p.CustomerID = c.CustomerID;

view created.

SELECT * FROM ProductCustomer;
